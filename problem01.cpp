// main execution of UniqueNumbers program
#include <iostream>
#include <iomanip>
#include "UniqueNumbers.h"
using namespace std;

int main()
{
	UniqueNumbers un;		// create UniqueNumbers object

	un.getNumber();
	un.fillArray(numbers, n);
	un.sortArray(numbers);
	un.countMembers(numbers);
	un.uniqueElements(numbers, elements, u, size);
	un.countIndividuals(numbers, elements, u, size);
}
